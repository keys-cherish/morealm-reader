# 贡献指南

感谢你对 MoRealm 的兴趣。这份指南说明：你能怎么参与，什么样的贡献能登上「贡献者」名单，以及如何在保护隐私的前提下被记录。

---

## 一、我能怎么帮 MoRealm？

我们认可 5 类贡献，对应 App 内「贡献者」页的 chip 标签：

| 维度 | 说明 |
|------|------|
| **Code** | 代码与架构：写功能、改 Bug、做重构、优化性能 |
| **Design** | 设计与体验：图标、配色、UX 方案、视觉资产 |
| **Issues** | 测试与反馈：在 GitHub 上提交可复现的高质量 Bug 报告 |
| **Community** | 社区贡献：在酷安、QQ 群、TG 等渠道长期答疑 / 内测 / 高质量反馈 |
| **Localization** | 多语言翻译 |

> Community 这一类是为了让没有 GitHub 账号的国内深度用户也能登榜 —— 在国内项目里，酷安评论区、QQ 群里的优质反馈往往不比 GitHub 少。

---

## 二、入榜标准

### Code & Architecture

满足以下任意一条：

- 至少一个被合并到主分支的实质性 PR（不计纯改错别字、纯格式化）
- 主导或深度参与某个核心模块的重构（仿真翻页 / 书源引擎 / TTS 等）
- 修复一个被标记为 high-priority 或 hard 的底层 Bug

### Design & UX

满足以下任意一条：

- 提供并被采纳的视觉资产（应用 Icon、内置主题、占位图等）
- 提交完整的 UX 优化方案（线框图 / 视觉稿）并被实现
- *仅在 Issue 里说「我觉得这里不好看」而无具体方案的，归类为 Issues 而非 Design*

### Testing & Issues

满足以下任意一条：

- 累计提交 ≥ 3 个高质量 Bug Issue（包含清晰的复现步骤、机型 / 系统信息、必要的日志）
- 在 Issue 区或讨论区多次（≥ 5 次）有效协助其他用户排查问题、验证 Bug 修复

### Community

由核心维护者背书，且至少满足以下一条：

- 在 QQ 群 / 酷安 / TG 群等非 GitHub 渠道长期提供高质量反馈（有可追溯的截图 / 群消息记录）
- 长期协助新用户答疑、引导他们填写有效 Bug 报告
- 持续参与内测并给出建设性意见

### Localization

至少一种语言的完整翻译被合并到主仓库。

### 不计入贡献的情况

- 「我觉得这个 App 应该加 XX 功能」式的纯诉求，没有具体方案 / 调研
- 「闪退了」「用不了」之类没有任何排查信息的无效 Issue
- 通过自动化工具（如 dependabot 之外的脚本）批量提交的格式化 PR

---

## 三、隐私保护

提交贡献者信息时，**所有字段都由你本人决定是否展示**。我们的默认策略：

| 字段 | 默认 | 备注 |
|------|------|------|
| `name` | 必填 | 可以是真名，也可以是昵称、ID |
| `avatar` | 可选 | 不填会自动生成首字母彩色圆 |
| GitHub handle | 可选 | 显示为 `@username` + 跳链接 |
| 酷安 handle | 可选 | 同上 |
| QQ | **不显示数字** | 只能写「群名片：xxx」格式，不接受裸 QQ 号 |
| 邮箱 | 可选 | 默认转换为 `mailto:` 链接 |
| Telegram | 可选 | 显示为 `@username` + 跳链接 |
| `contribution` | 必填 | 一句话描述你做了什么，会显示在卡片上 |

如果你随时希望从名单中移除自己，只需提交一个 PR 删除对应条目，或私信维护者，**不需要提供任何理由，不会留下记录**。

---

## 四、提交流程

### 如果你有 GitHub 账号

1. Fork 本仓库
2. 编辑 `app/src/main/assets/contributors.json`，按现有格式追加你的条目
3. 提交 PR，附上你想被记录的贡献描述

`contributors.json` 单条目格式：

```json
{
  "id": "your-slug",
  "name": "你的展示名",
  "avatar": "https://...（可选）",
  "tags": ["Code", "Design"],
  "contribution": "你做了什么（一句话）",
  "joinedAt": "2026-05-07",
  "links": [
    {
      "platform": "github",
      "handle": "@your-id",
      "url": "https://github.com/your-id"
    }
  ]
}
```

支持的 `platform` 值：`github` / `coolapk` / `qq` / `telegram` / `email`。其它字符串会以通用图标显示。

### 如果你没有 GitHub 账号

通过以下任意方式联系维护者，告诉我们：

- 你的展示昵称（必填）
- 你想展示的平台 handle（如「酷安：@xxx」「TG：@xxx」「QQ 群名片：xxx」）
- 一句话贡献描述

联系渠道：

- **App 内** → 我的 → 关于墨境 → 联系我们（QQ 群）
- **GitHub Issue** → 标题前缀 `[contributors]`（哪怕只是开个 Issue 写信息也行，我们会代你提 PR）
- **邮箱** → 在 App 里联系作者获取最新邮箱

---

## 五、行为底线

无论之前贡献多少，出现以下任一情况会被从名单中**永久移除**：

- 恶意攻击其他贡献者或用户
- 抄袭他人代码 / 设计资产
- 故意引战、传播虚假信息
- 利用项目影响力进行营销 / 政治宣传

---

## 六、其他协作约定

- **不强制贡献者签 CLA**。代码贡献按现有 GPL-3.0 + 商业双许可证授权
- **首次贡献者** 提的 PR 我们会优先 review，不要担心代码不够好
- **想做大的改动**（新增模块、改架构等）建议先开 Issue 讨论方向再写代码

---

如果这份指南有任何不清楚的地方，欢迎开 Issue 反馈。
